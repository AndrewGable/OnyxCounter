export default {
  // Onyx key that stores the current count
  COUNTER: 'counter',

  // Onyx key that stores an array of number facts
  NUMBER_FACTS: 'numberFacts',

  // Onyx collections
  COLLECTION: {
    // Onyx collection that stores both cat and dog facts
    FACTS: 'facts_',
  },
};
